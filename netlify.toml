[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  # Vite 7 requires Node 20.19+ — builds fail on Node 18
  NODE_VERSION = "22"